name: "Terraform Infrastructure Pipeline"

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - prod

permissions:
  contents: read
  id-token: write
  pull-requests: write

env:
  TERRAFORM_VERSION: "1.6.0"

jobs:
  terraform-dev:
    name: "Terraform Dev"
    runs-on: ubuntu-latest
    environment: dev
    if: github.event_name != 'workflow_dispatch'
    env:
      TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
      TF_VAR_environment: dev
      ROLE_ARN: ${{ vars.AWS_ROLE_ARN }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: us-east-1
          role-to-assume: ${{ env.ROLE_ARN }}
          role-session-name: terraform-dev

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: Terraform Format
        id: fmt
        run: terraform fmt -check

      - name: Terraform Init
        id: init
        run: |
          terraform init \
            -backend-config="bucket=${{ vars.TF_STATE_BUCKET }}" \
            -backend-config="key=dev/terraform.tfstate"

      - name: Terraform Validate
        id: validate
        run: terraform validate -no-color

      # ── Policy-as-Code: tfsec analiza el plan en busca de configuraciones inseguras ──
      - name: tfsec – Escaneo de políticas de seguridad (Policy-as-Code)
        uses: aquasecurity/tfsec-action@v1.0.3
        with:
          working_directory: .
          soft_fail: false

      # ── Mínimo privilegio: rechazar si algún rol usa AdministratorAccess ──
      - name: Auditar principio de mínimo privilegio (IAM)
        run: |
          echo "Verificando que ningún recurso Terraform use AdministratorAccess..."
          if grep -r "AdministratorAccess" --include='*.tf' modules/ main.tf; then
            echo "✗ Se detectó AdministratorAccess en la infraestructura. El deploy se bloquea."
            exit 1
          fi
          echo "✓ Ningún rol contiene AdministratorAccess – mínimo privilegio validado"

      # ── Gestión de secretos: verificar que no haya credenciales en texto plano ──
      - name: Verificar ausencia de secretos hardcodeados en Terraform
        run: |
          echo "Buscando credenciales en texto plano en archivos .tf..."
          if grep -rEin '(password|secret_key|access_key)\s*=\s*"[^$][^"]*"' --include='*.tf' modules/ main.tf environments/; then
            echo "✗ Se detectaron posibles credenciales hardcodeadas en Terraform."
            exit 1
          fi
          echo "✓ Los secretos se gestionan vía variables sensibles y Secrets Manager"

      - name: Terraform Plan
        id: plan
        if: github.event_name == 'pull_request'
        run: terraform plan -no-color -var-file="terraform.tfvars"
        continue-on-error: true

      - name: Update Pull Request
        uses: actions/github-script@v7
        if: github.event_name == 'pull_request'
        env:
          PLAN: "terraform\n${{ steps.plan.outputs.stdout }}"
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const output = `#### Terraform Format 🖌\`${{ steps.fmt.outcome }}\`
            #### Terraform Init\`${{ steps.init.outcome }}\`
            #### Terraform Validate\`${{ steps.validate.outcome }}\`
            #### Terraform Plan\`${{ steps.plan.outcome }}\`

            <details><summary>Show Plan</summary>

            \`\`\`
            ${process.env.PLAN}
            \`\`\`

            </details>

            *Pushed by: @${{ github.actor }}, Action: \`${{ github.event_name }}\`*`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            })

      - name: Terraform Plan Status
        if: steps.plan.outcome == 'failure'
        run: exit 1

      - name: Terraform Apply
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: terraform apply -auto-approve -input=false -lock=false -var-file="environments/dev/terraform.tfvars"

  # terraform-prod:
  #   name: "Terraform Prod"
  #   runs-on: ubuntu-latest
  #   environment: prod
  #   env:
  #     TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
  #     TF_VAR_environment: prod
  #     ROLE_ARN: ${{ vars.AWS_PROD_ROLE_ARN }}

  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0
  #         sparse-checkout: |
  #           environments
  #           main.tf
  #           providers.tf
  #           variables.tf
  #           outputs.tf
  #           versions.tf
  #           modules

  #     - name: Configure AWS Credentials (OIDC)
  #       uses: aws-actions/configure-aws-credentials@v4
  #       with:
  #         aws-region: us-east-1
  #         role-to-assume: ${{ env.ROLE_ARN }}
  #         role-session-name: terraform-prod

  #     - name: Setup Terraform
  #       uses: hashicorp/setup-terraform@v3
  #       with:
  #         terraform_version: ${{ env.TERRAFORM_VERSION }}

  #     - name: Terraform Init
  #       id: init
  #       run: |
  #         terraform init \
  #           -backend-config="bucket=${{ vars.TF_STATE_BUCKET }}" \
  #           -backend-config="key=prod/terraform.tfstate"

  #     - name: Terraform Validate
  #       id: validate
  #       run: terraform validate -no-color

  #     - name: Terraform Plan
  #       id: plan
  #       if: github.event_name == 'pull_request'
  #       run: terraform plan -no-color -var-file="terraform.tfvars"
  #       continue-on-error: true

  #     - name: Terraform Apply
  #       if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #       run: terraform apply -auto-approve -input=false -var-file="terraform.tfvars"

  rollback:
    name: "Rollback ${{ github.event.inputs.environment }}"
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    if: github.event_name == 'workflow_dispatch'
    env:
      TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
      TF_VAR_environment: ${{ github.event.inputs.environment }}
      ROLE_ARN: ${{ vars.AWS_ROLE_ARN }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: us-east-1
          role-to-assume: ${{ env.ROLE_ARN }}
          role-session-name: terraform-rollback

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: Terraform Init
        run: |
          terraform init \
            -backend-config="bucket=${{ vars.TF_STATE_BUCKET }}" \
            -backend-config="key=${{ github.event.inputs.environment }}/terraform.tfstate"

      - name: Terraform Destroy
        run: terraform destroy -auto-approve -var-file="environments/dev/terraform.tfvars"
